from fastapi import APIRouter

router = APIRouter(prefix="/api/vendor/analytics", tags=["Vendor Analytics"])

# Intern 2 to implement endpoints here
# Refer to app/schemas/analytics_schema.py for response models
